import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const api_Constants = () => {
  return (
    <View>
      <Text>new component created...</Text>
    </View>
  )
}

export default api_Constants

const styles = StyleSheet.create({})
